## Libraries

```
atexit: register functions to call when your program exits
argparse: for parsing command line arguments
bisect: bisection algorithms for sorting lists
calendar: date-related functions
codecs: for encoding and decoding data
collections: a variety of useful data structures (named tuples)
copy: for copying data
csv: for reading and writing CSV files
datetime: classes for handling dates and times
fnmatch: for matching Unix-style filename patterns
glob: for matching Unix-style path patterns
io: for handling I/O streams
StringIO: treat strings as files
json: for reading and writing data in JSON format
logging: logging functionality
multiprocessing: run multiple sub-processes from your application while providing an API that makes them look like threads
operator: lambda expressions
os: basic OS functions
random: for generating pseudo-random numbers
re: regular expressions
select: select() and poll() functions for creating event loops
shutil: high-level file functions
signal: for handling POSIX signals
tempfile: for creating temporary files and directories
threading: high-level threading functionality
urllib: for handling and parsing URLs
uuid: generate UUIDs (Universally Unique Identifiers)
```

```
funtools
memory-profiler
Hy
cProfile
pyprof2calltree
timeit
tox, detox
coverage
testrepository
subunit
testscenarios
```

```
Blaze
beutifulsoup
Bokeh
click
configparser
cryptography
curl
dask
dask.distributed
flask
html5lib
```

## Frameworks

```
# Note: web
Django
Pylons
TurboGears
Tornado
Zope
Plone
```

```
# Note: event driven
Twisted
Circuits
```

[The Hacker's Guide to Python Libraries](https://docs.python-guide.org/)
