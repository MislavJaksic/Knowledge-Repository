REPORT AlgebraAndStrings.

*Data objects need to be defined before use
DATA integer TYPE i.
DATA float TYPE f.
DATA character TYPE c.
DATA string TYPE string.
DATA date TYPE d. 
DATA time TYPE t.

*Normal division
integer = 6 / 5.
WRITE / integer. "-> _________1

*Float division (at least one needs to be float)
float = 6 / 5.
WRITE / float. "-> __1,2000000000000000E+00
*Modulo
integer = 15 MOD 2.
WRITE / integer. "-> _________1

*Strings
string = 'String'.
*String length
integer = STRLEN( string ).
WRITE / integer. "-> _________6
*String function (one of many)
REPLACE 'i' WITH ' ' INTO string.
WRITE / string. "-> Str ng