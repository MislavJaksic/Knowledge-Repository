Quick sort
====================

Quick sort is a complex algorithm that swaps elements accross a sentinel value, most often a median, an element in the middle. Its speed is O(nlogn), but its worste case is O(n*n).